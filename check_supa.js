import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.EXPO_PUBLIC_SUPABASE_URL, process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from('profiles').select('id, first_name, last_name, role'); console.log('error:', error); console.log('Total profiles:', data?.length); const { data: d2, error: e2 } = await supabase.from('profiles').select('*').not('role', 'in', ['Sistem Yöneticisi', 'Sekreter', 'Güvenlik Görevlisi']); console.log('With not-in-array length:', d2?.length, e2); const { data: d3, error: e3 } = await supabase.from('profiles').select('*').neq('role', 'Sistem Yöneticisi').neq('role', 'Sekreter').neq('role', 'Güvenlik Görevlisi'); console.log('With neq chain:', d3?.length, e3); } run();
